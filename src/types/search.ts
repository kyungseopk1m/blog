export interface SearchDocument {
  slug: string;
  title: string;
  description: string;
  content: string;
  category: string;
  pubDate: string;
}

export interface SearchResult {
  slug: string;
  title: string;
  description: string;
  category: string;
  pubDate: string;
}
